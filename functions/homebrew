# -*-shell-script-*-

#
# Return the path of a homebrew package
#

brew-path () {
  [ -x $(which brew) ] && dirname $(brew list $1 | head -1)
}

#
# Find files in a homebrew package
#

brew-file () {
  [ -x $(which brew) ] && brew list $1 | grep $2
}
